# STAGE_TWO_SUCCESSFUL_EXPERIMENTS_ARCHITECTURE_INTEGRATION_003 — ST2-EXP-004 Learning-to-Construction Act

**Document ID:** `BOMA-ST2-LEARNING-INTEGRATION-003`  
**Date:** `2026-08-25`  
**Status:** `OWNER-AUTHORIZED ARCHITECTURE ACT / ST2-EXP-004 LESSON INTEGRATED / NOT A NEW EXPERIMENT`  
**Experiment:** `ST2-EXP-004 / CLOSED / PASS / EXACT F-04 IMPACT CLASSIFIED`  
**Architectural origin:** `DECISION_POINT / R-DP-003`  
**Accepted exports:** unchanged  
**Selected routes:** unchanged  
**New Block / Decision Point / Junction:** none.

## 1. Purpose

Convert the verified architectural lesson from `ST2-EXP-004` from closed
Learning-Graph evidence into permanent Construction-DAG knowledge without
promoting the experiment's alternative assemblies, changing the accepted real
carrier/order/operations, or changing the selected logical regime at
`R-DP-003`.

The transition is:

```text
ST2-EXP-004 closed experimental evidence
        ↓
exact F-04 proof/package impact classification
+ same-carrier totality boundary
+ downstream C sensitivity classification
        ↓
permanent architectural knowledge attached to existing R-DP-003
```

and explicitly not:

```text
PASS                         → constructive accepted R
no-F04 research assembly     → accepted R replacement
conditional CutComparability → constructive recovery
proof ancestry               → mathematical necessity
located-cut proposal         → same frozen representation
shared downstream survival   → new Junction
```

## 2. Exact research and merge provenance

Frozen accepted reference:

```text
50f3031b8d2657cbe0710e73e5935d997d40e49b
```

Frozen accepted tree:

```text
e4842acdf2b08c8db54b45d0798c36ee7565f351
```

Immutable Frozen Plan commit:

```text
89c9dc9154e7ca469e5c94c177be223205ee9dbd
```

Final Study/Act commit:

```text
6779d028c49f73757ea838c163d3968a982559fe
```

Final lifecycle-closed exact research head after read-only CI cleanup:

```text
1fe760de811ad2b176ead6f420b80ca1aab5ce46
```

On that exact head the ST2-EXP-004 historical/read-only checks passed:

```text
Gate A     run 32846022246  artifact 9562318338
Gate B     run 32846022237  artifact 9562319856
Gate C     run 32846022351  artifact 9562323718
Gate D     run 32846022560  artifact 9562324905
Gate E     run 32846022280  artifact 9562318037
Gate F     run 32846022327  artifact 9562321964
Lifecycle  run 32846022274  artifact 9562318844
```

The historical sentinels explicitly perform no scientific re-execution. Their
role is to preserve the immutable Gate records, exact final-run identities,
Frozen Plan, accepted-control immutability, and closed lifecycle state.

PR #16 merged the exact verified research tree into `main` as:

```text
merge commit  61adb8589c803e95e1b96ef38902320c8aa5df19
parent 1      c26854e551c7e6245ef9c13c01a2424def6bfab5
parent 2      1fe760de811ad2b176ead6f420b80ca1aab5ce46
merge tree    256b4dce1bdbb93858b28d28ece7a9e177a1685c
```

A direct comparison from the exact research head to the merge commit has zero
changed files. The merge adds ancestry only; it does not alter the verified
research content.

## 3. Integrated architectural fact — exact same-carrier totality boundary

The accepted Dedekind representation remains:

```text
LowerCut
CutEquiv
cutSetoid
RBOMA := Quotient cutSetoid
CutLE
rLE
```

Its constructive order core proves:

```text
rLE reflexivity
rLE transitivity
rLE antisymmetry
```

The selected Stage-I F-04 provider supplies:

```text
CutComparability := ∀ A B : LowerCut, CutLE A B ∨ CutLE B A
```

and the constructive bridge then yields quotient totality.

Gate D established on this **unchanged carrier and unchanged order relation**:

```text
RTotality ↔ CutComparability
```

where `RTotality` denotes:

```text
∀ x y : RBOMA, rLE x y ∨ rLE y x
```

This equivalence is now permanent architectural knowledge attached to the
existing `R-DP-003`.

## 4. What was not recovered

No unconditional constructive inhabitant of `CutComparability` was recovered
from the frozen `LowerCut` interface.

Therefore the experiment does **not** justify any of the following claims:

```text
accepted R totality is now constructive
F-04 is mathematically impossible to eliminate
CutComparability follows constructively from the current LowerCut fields
all classical commitments in accepted R disappeared
```

A located-cut or witness-carrying redesign may be a meaningful future research
candidate, but changing the cut specification changes the representation and
was outside the single-factor boundary of `ST2-EXP-004`.

## 5. Exact F-04 impact is proof/package impact, not necessity

Gate A measured the current declaration closures as:

```text
F04_DIRECT             8
F04_TRANSITIVE         7
F04_FREE              22
OTHER_CLASSICAL_ONLY  18
unresolved             0
internal axioms        0
```

This classification records exact formal dependency in the measured current
implementation. It does not convert formal ancestry into a theorem of
mathematical necessity.

Gate B independently showed the source-packaging dimension: a whole-source
no-F04 research assembly retained `77 / 88` accepted-manifest R source files.
That number is a packaging/survivor measurement. A source file can fail to
survive because it contains an F-04-sensitive declaration even when another
declaration in the same file is mathematically F-04-free.

Permanent rule:

```text
declaration dependency ≠ whole-source elaboration dependency
formal dependency        ≠ mathematical necessity
```

## 6. Independent logical commitments remain visible

The changed factor was only F-04:

```text
cutComparability_classical
rLE_total_classical
```

Independent localized commitments remained controls:

```text
F-05  finite membership / finite cut bracketing provider
F-06  positive representative extraction provider
F-07  rational-density provider
```

Consequently:

```text
NO F-04 dependency ≠ fully constructive R
```

This distinction is permanent Construction-DAG knowledge and must be preserved
in future dependency or logical-cost summaries.

## 7. Downstream C sensitivity learned from removing only orderTotal

`ST2-EXP-001` established the exact sixteen-property mathematical production
surface from accepted R into selected C. Gate E of `ST2-EXP-004` removed
**exactly one field** from that interface:

```text
orderTotal
```

Seven accepted C Claim families survived the measured fifteen-property
alternative:

```text
C-CL-CARRIER-001
C-CL-REMBED-001
C-CL-I-001
C-CL-GEN-001
C-CL-COORDUNIQ-001
C-CL-NONREAL-001
C-CL-COMPARE-001
```

The current proof closures for:

```text
C-CL-FIELD-001
C-CL-INTEGRATION-001
```

did not survive. Their measured dependence is transitive through the current
square/nonnegative → norm → field path. This is a statement about the current
formal proof architecture, not a proof that every construction of those C
meanings mathematically requires total order.

`C-CL-COMPARE-001` survives, consistently with the separately integrated
`ST2-EXP-011` comparison boundary:

```text
zero / one / neg / add / mul
+ explicit quadratic coordinate laws
```

Thus production sensitivity and comparison sensitivity remain distinct.

## 8. Permanent interpretation of R-DP-003

`R-DP-003` remains resolved exactly as before:

```text
constructive partial-order core
+
localized classical F-04 witness of CutComparability
+
constructive totality-from-CutComparability bridge
```

The new permanent knowledge is the measured boundary around that selection:

```text
same-carrier totality is equivalent to CutComparability;
F-04 has a precisely measured direct/transitive current proof impact;
substantial R and C closures are F-04-free;
other classical commitments remain independent;
no unconditional constructive CutComparability was recovered;
a representation-changing locatedness redesign is a different candidate.
```

`SELECTS` remains a methodological/project choice. The experiment refines the
knowledge attached to the Decision Point; it does not reverse or replace the
Decision Point.

## 9. What this Act changes

Permanent current-state architecture now records:

```text
R-DP-003
  selected regime unchanged
  + exact RTotality ↔ CutComparability boundary
  + exact F-04 proof/package impact classification
  + explicit F-05/F-06/F-07 independence warning
  + downstream orderTotal sensitivity knowledge
  + representation-change boundary for future located-cut work
```

The Construction DAG, registry, graph views, and current Stage-Two handoff state
are synchronized to this fact.

## 10. What this Act does not change

```text
R-DP-001 SELECTS                         R-ROUTE-D / Dedekind
R-DP-002 identity                        cutSetoid quotient
R-DP-003 SELECTS logical regime          localized classical comparability
accepted R export                       R-BLOCK-001
BOMA-C-R-DEP-001 production surface     exact sixteen-property interface
C-DP-001 SELECTS                         C-ROUTE-P
selected C producer                     C-BLOCK-001
C-COMPARE-BLOCK-001 comparison surface  ST2-EXP-011 integrated boundary
accepted C integration                  C-J-001
accepted C export                       C-BLOCK-002 / CA-20
R-ROUTE-C / Cauchy                      verified non-selected alternative
C-ROUTE-Q                               verified non-selected alternative
```

No accepted Q/R/C Lean source or accepted manifest is modified by this Act.
The research-only `ST2Exp004*.lean` witnesses remain Learning-Graph evidence and
are not promoted to accepted producers.

## 11. No new canonical unit is justified

The experiment began at an already existing Decision Point and learned a sharper
boundary around that Decision Point. It did not produce an independent accepted
producer or a new reconvergence object.

Therefore:

```text
new Block          NONE
new Decision Point NONE
new Junction       NONE
```

Creating any of those only to display the lesson would distort the BOMA
ontology.

## 12. Stage-Two frontier after this integration

After this Act is merged and `main` is synchronized:

```text
ST2-EXP-001  CLOSED / PASS / LESSON INTEGRATED
ST2-EXP-002  CLOSED / PASS / LESSON INTEGRATED
ST2-EXP-003  CLOSED / PASS / LESSON INTEGRATED
ST2-EXP-011  CLOSED / PASS / LESSON INTEGRATED
ST2-EXP-004  CLOSED / PASS / LESSON INTEGRATED
ACTIVE EXPERIMENT = NONE
AUTONOMOUS RESEARCH PROGRAM = NO_ACTIVE_PROGRAM
NEXT EXPERIMENT = NOT AUTHORIZED
REQUIRED NEXT ACT = STOP BEFORE NEW EXPERIMENT / OWNER AUTHORIZATION REQUIRED
NO NEW EXPERIMENT
```

No new experiment, candidate activation, branch, or Frozen Plan is authorized by
this integration sequence.