# STAGE_TWO_SUCCESSFUL_EXPERIMENTS_ARCHITECTURE_INTEGRATION_002 — ST2-EXP-011 Learning-to-Construction Act

**Document ID:** `BOMA-ST2-LEARNING-INTEGRATION-002`  
**Date:** `2026-08-24`  
**Status:** `OWNER-AUTHORIZED ARCHITECTURE ACT / ST2-EXP-011 LESSON INTEGRATED / NOT A NEW EXPERIMENT`  
**Experiment:** `ST2-EXP-011 / CLOSED / PASS`  
**Architectural origin:** `BLOCK / C-COMPARE-BLOCK-001`  
**Accepted exports:** unchanged  
**Selected routes:** unchanged  
**New Block / Decision Point / Junction:** none.

## 1. Purpose

Convert the verified architectural lesson from `ST2-EXP-011` from closed
Learning-Graph evidence into permanent Construction-DAG knowledge without
promoting the experimental Lean implementation or changing accepted C.

This Act follows the general Learning-to-Construction rule established by
`BOMA-ST2-LEARNING-INTEGRATION-001` but is a distinct integration epoch. The
first Act remains the historical authority for `ST2-EXP-001..003`; this Act is
the authority for the `ST2-EXP-011` comparison-interface lesson.

The transition is:

```text
ST2-EXP-011 closed experimental evidence
        ↓
verified minimal comparison dependency surface
        ↓
permanent architectural fact inside C-COMPARE-BLOCK-001
```

and explicitly not:

```text
research implementation → accepted replacement
RCBOMA/H6                → accepted C producer
shared interface         → fabricated Junction
PASS                     → change of SELECTS
```

## 2. Exact merge and verification provenance

The experiment was frozen from accepted `main`:

```text
b49826e58261a71634cd12756e8afda28920088f
```

Immutable Frozen Plan commit:

```text
5cc05ef1bea8e88787041c3bc30dd0aecf5412d4
```

Final lifecycle-closed exact verification head:

```text
632a7134f26daf9dd781e3546804941f429a4246
```

On that exact head all nine relevant PR workflows passed. The ST2-EXP-011
workflow evidence is:

```text
run       32754345656
artifact  9530261359
sha256    d93c6f1ec34858f6cbc1556e92b86a241f6399e6a3cf894204608a51d63de2e5
result    SUCCESS
```

PR #13 merged that exact verified tree into `main` as:

```text
merge commit 72394878854aa69e865d17567959bec1daa70e6d
parent 1     b49826e58261a71634cd12756e8afda28920088f
parent 2     632a7134f26daf9dd781e3546804941f429a4246
merge tree   168fc1267313810e622c2206fa8a91f24decb4b7
```

A direct comparison from the verified experiment head to the merge commit has
zero changed files. The merge therefore adds ancestry only; there is no
post-verification file-content drift.

## 3. Integrated architectural fact — comparison scalar surface

Gate A and the exact declaration-closure audits established that the quadratic
comparison proof does **not** require the whole accepted real integration
certificate and does **not** require the sixteen-property production R→C
surface established by `ST2-EXP-001`.

The direct scalar-operation surface required by the comparison layer is:

```text
zero
one
neg
add
mul
```

Together with the quadratic presentation's explicit coordinate laws:

```text
coord
coordinateGeneration
coordinateUnique
coordinateZero
coordinateOne
coordinateReal
coordinateImag
coordinateNeg
coordinateAdd
coordinateMul
```

This is now permanent architectural knowledge attached to the existing
`C-COMPARE-BLOCK-001` and its direct Claim `C-CL-COMPARE-001`.

No scalar order law, completeness law, density law, Archimedean law, general
field-law bundle, or inverse-existence/uniqueness field belongs to the direct
comparison proof closure. Inverse-witness preservation transports an already
supplied witness; it does not construct or select an inverse.

## 4. Two distinct R→C surfaces must remain distinct

BOMA now has evidence for two different dependency questions:

```text
PRODUCTION C MATHEMATICAL SURFACE — ST2-EXP-001
  exact sixteen-property ordered-field/witness interface
  authority: BOMA-C-R-DEP-001

QUADRATIC COMPARISON SURFACE — ST2-EXP-011
  scalar zero/one/neg/add/mul
  + explicit quadratic coordinate laws
  authority: C-COMPARE-BLOCK-001 / C-CL-COMPARE-001
```

The second surface is narrower because it answers a narrower question. It does
not revise the production contract and must not be used to claim that the
selected C producer as a whole depends only on five scalar operations.

Conversely, the sixteen-property production surface must not be projected onto
the comparison Block as mathematical necessity merely because the accepted
formal implementation currently has that ancestry available.

## 5. Accepted RBOMA semantics are preserved

The accepted-RBOMA adapter proved that the generic relation has the same
shared-coordinate meaning as the existing accepted comparison relation. The
key theorem `related_iff_accepted` is definitionally proved by `rfl`.

Therefore the integrated lesson is not a semantic replacement of
`C-CL-COMPARE-001`. It is a dependency/interface refinement of how that meaning
can be factored.

The accepted comparison source, accepted manifests, selected route, integration
Junction, and accepted export remain unchanged by this Act.

## 6. Native RCBOMA/H6 compatibility is permanent robustness knowledge

`ST2-EXP-011` also verified a native comparison instance over the independently
constructed Cauchy real/H6 complex research route:

```text
RCBOMA + H6 C carrier
        ↓
ST2-EXP-011 generic comparison interface
```

The native implementation closure excludes H5 `cToD` transport and selected
Dedekind/RStage-integration implementation sources.

The permanent architectural conclusion is:

```text
H5/Dedekind transport is not a mathematical implementation dependency of the
quadratic comparison meaning once the comparison scalar boundary is factored
through its verified generic interface.
```

This is compatibility/robustness knowledge. It does not make `RCBOMA`, H6, or
the experimental adapter accepted.

## 7. Relation/function firewall remains part of the permanent boundary

The experiment preserved the distinction:

```text
relation-level totality + uniqueness
!=
chosen functional comparison
```

Functional maps require explicit `CoordinateExtractor` data. No global
coordinate selector, inverse selector, or convenience `Classical.choose` was
introduced in the generic comparison layer.

This firewall is now part of the documented architectural contract of
`C-COMPARE-BLOCK-001`.

## 8. Logical-commitment interpretation

Exact kernel/dependency evidence classified the roots as:

```text
generic comparison roots   NO AXIOMS
native H6 roots             [propext, Quot.sound]
accepted RBOMA adapter      [propext, Classical.choice, Quot.sound]
```

The accepted-adapter `Classical.choice` cost is inherited accepted formal
provenance, not a requirement introduced by the generic comparison interface.

Architectural dependency classification follows the minimal declarations
actually consumed by the comparison proof, not the largest ancestry available
to the selected producer.

## 9. What this Act changes

Permanent current-state architecture now records:

```text
C-COMPARE-BLOCK-001
  comparison scalar dependency
    = zero/one/neg/add/mul + explicit quadratic coordinate laws

  accepted RBOMA instance
    = same Related semantics

  native RCBOMA/H6 instance
    = verified possible without H5/Dedekind implementation transport

  functional comparison
    = only with explicit CoordinateExtractor
```

The Learning Graph continues to retain the Frozen Plan, failed runs, exact
artifacts, Study/Act, lifecycle closure, and CI-governance corrections that
established these facts.

## 10. What this Act does not change

```text
R-DP-001 SELECTS                         R-ROUTE-D / Dedekind
accepted R export                       R-BLOCK-001
BOMA-C-R-DEP-001 production surface     exact sixteen-property interface
C-DP-001 SELECTS                         C-ROUTE-P
selected C producer                     C-BLOCK-001
accepted comparison Claim meaning       C-CL-COMPARE-001 unchanged
accepted C integration                  C-J-001
accepted C export                       C-BLOCK-002 / CA-20
R-ROUTE-C / Cauchy                      non-selected verified alternative
H6 Cauchy-native C                      non-accepted research evidence
C-ROUTE-Q                               non-selected verified alternative
ST2-EXP-002/003 research Junctions       non-acceptance
```

No new canonical Block, Decision Point, or Junction is justified by 011.

## 11. Experimental code disposition

The three `ST2Exp011*.lean` research sources and their research manifests remain
verified experiment evidence. This Act does not silently replace accepted
`CQuadraticComparison001` with them.

The generic factoring is a **canonical-refactor candidate**, not a completed
accepted-source refactor. A future implementation cleanup may adopt it only
through a separately controlled change with accepted-source regression evidence.

## 12. Stage-Two frontier after this integration

After this Act is merged and `main` is synchronized/verified:

```text
ST2-EXP-001  CLOSED / PASS / LESSON INTEGRATED
ST2-EXP-002  CLOSED / PASS / LESSON INTEGRATED
ST2-EXP-003  CLOSED / PASS / LESSON INTEGRATED
ST2-EXP-011  CLOSED / PASS / LESSON INTEGRATED
ACTIVE EXPERIMENT = NONE
```

The owner-sequenced next experiment is `ST2-EXP-004`, but it remains
**not active** and has **no Frozen Plan** at this integration stage.

Before Do may begin:

```text
merge this architecture Act
→ synchronize and verify current main
→ re-read current architecture/main
→ prepare an independent ST2-EXP-004 Frozen Plan from that exact main
→ open a separate ST2-EXP-004 branch
```

Its single changed factor is the logical regime at `R-DP-003`; no artifact or
Plan from ST2-EXP-011 may be reused as its experimental Plan.
