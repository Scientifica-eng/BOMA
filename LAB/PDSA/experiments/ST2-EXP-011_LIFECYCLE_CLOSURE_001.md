# ST2-EXP-011_LIFECYCLE_CLOSURE_001 — Owner-authorized lifecycle closure

**Document ID:** `BOMA-ST2-EXP-011-LIFECYCLE-CLOSURE-001`  
**Date:** `2026-08-24`  
**Status:** `CLOSED / PASS / OWNER AUTHORIZED`  
**Experiment:** `ST2-EXP-011 / C Comparison Interface Generalization`  
**Architectural origin:** `BLOCK / C-COMPARE-BLOCK-001`  
**Immutable Frozen Plan:** `LAB/PDSA/PDSA-ST2-EXP-011_C_COMPARISON_INTERFACE_GENERALIZATION.md`  
**Frozen accepted reference:** `b49826e58261a71634cd12756e8afda28920088f`  
**Frozen Plan commit:** `5cc05ef1bea8e88787041c3bc30dd0aecf5412d4`

## 1. Closure authorization

The owner explicitly selected `ST2-EXP-011`, required a separately frozen Plan,
required Do/Study/Act and lifecycle disposition before any next experiment, and
authorized integration of durable learning only after lifecycle closure.

The mathematical Study/Act completed with PASS in:

`LAB/PDSA/experiments/ST2-EXP-011_FINAL_STUDY_ACT_001.md`.

This record executes that already-authorized lifecycle transition. It is not an
automatic acceptance promotion, not a canonical implementation refactor, and
not a retroactive edit of the immutable Frozen Plan or Study/Act.

## 2. Scientific result being closed

The experiment changed exactly one scientific factor:

```text
C quadratic comparison scalar abstraction
```

It did not change the accepted real producer, accepted complex producer,
selected routes, carrier choices, or the R total-order logical regime.

The exact comparison proof closure was found to require only scalar operations:

```text
zero
one
neg
add
mul
```

plus explicit quadratic-coordinate data/laws:

```text
coord
coordinateGeneration
coordinateUnique
coordinateZero
coordinateOne
coordinateReal
coordinateImag
coordinateNeg
coordinateAdd
coordinateMul
```

The generic relation/certificate layer is therefore mathematically narrower
than both the whole accepted `RStageIntegrationCertificate` formal package and
the sixteen-property production R→C surface established by ST2-EXP-001.

## 3. Verified implementations

The research lineage contains three deliberately separated layers:

```text
ST2Exp011GenericQuadraticComparison.lean
  generic scalar + quadratic-coordinate relation/certificate

ST2Exp011AcceptedRBOMAComparison.lean
  accepted RBOMA adapter; old/new Related semantics preserved definitionally

ST2Exp011CauchyH6Comparison.lean
  native RCBOMA/H6 adapter without H5/Dedekind implementation transport
```

The accepted-adapter theorem `related_iff_accepted` is proved by `rfl`.
The native H6 assembly rejects H5/Dedekind implementation declarations in its
dependency closure.

The relation/function firewall remains explicit:

```text
relation comparison
!=
functional comparison
```

Functions are constructed only from explicit `CoordinateExtractor` data. No
global coordinate selector, inverse selector, or convenience
`Classical.choose` was introduced in the generic comparison sources.

## 4. Logical-cost result

Kernel evidence distinguishes the comparison abstraction from inherited
producer provenance:

```text
generic quadraticComparison                 no axioms
generic constructiveQuadraticIsomorphism    no axioms
native H6 comparison roots                   [propext, Quot.sound]
accepted RBOMA adapter roots                 [propext, Classical.choice, Quot.sound]
```

The accepted adapter's larger logical ancestry is inherited formal provenance;
it is not a mathematical necessity of the generic comparison interface.

## 5. Exact green pre-closure verification head

After the Study/Act and the lifecycle-CI compatibility correction, the exact
branch head:

`ef116405c08475ec8702d9177a5106d7d0bfe525`

passed the full relevant PR-triggered matrix:

```text
ST2-EXP-011 C Comparison Interface Generalization
  run 32753140129  SUCCESS
  artifact 9529812715
  sha256 f7ec7a6d1fb88a8c59dbcc8ce04bd8d6c389a3b3d77bb6ed3c1f22dc629dd9b1

Stage-Two historical lifecycle closure preservation
  run 32753140144  SUCCESS

ST2-EXP-003 Independent Cauchy Real Route
  run 32753140147  SUCCESS

ST2-EXP-003 Cauchy Quadratic Rebuild
  run 32753140145  SUCCESS

ST2-EXP-003 Dedekind-Cauchy Comparison
  run 32753140218  SUCCESS

ST2-EXP-002 Independent Quotient Route and Pair Junction
  run 32753140135  SUCCESS

C Comparable Quadratic Route Probes
  run 32753140225  SUCCESS

C Same-Carrier Integration and Claim Transparency
  run 32753140141  SUCCESS

C Stage Acceptance and Six-Stage Transparency
  run 32753140262  SUCCESS
```

This exact head also preserves the Frozen Plan ancestor/immutability and the
accepted Q/R/C manifests and accepted manifest-listed sources.

## 6. Preserved failure evidence

Two failure mechanisms remain retained and must not be rewritten away:

### ERR-ST2-011-001

`LAB/PDSA/experiments/ST2-EXP-011_FAILURE_001_SOURCE_BOUNDARY_COMMENT_FALSE_POSITIVE.md`

A lexical source-boundary scan matched forbidden concrete names in a comment.
The correction changed only documentary text; no interface field or theorem was
widened.

### ERR-ST2-011-002

`LAB/PDSA/experiments/ST2-EXP-011_FAILURE_002_HISTORICAL_LIFECYCLE_CI_SCOPE.md`

Historical ST2-EXP-002/003 workflows incorrectly equated local historical
closure with a permanently empty global Stage-Two frontier. The correction
introduced explicit historical-closure governance while preserving the strict
original closure audit for reproduction of the historical frontier. A secondary
schema-compatibility failure was also retained before the full matrix became
green.

Neither failure changes the mathematical PASS result.

## 7. Claim and architecture disposition

Direct affected Claim:

`C-CL-COMPARE-001`.

Secondary audit Claim:

`C-CL-INTEGRATION-001`.

The experiment establishes durable architecture knowledge about the scalar
boundary inside the existing `C-COMPARE-BLOCK-001`. It does **not** justify a
new Block, Decision Point, or Junction merely because both RBOMA and RCBOMA/H6
instantiate the same interface.

In particular:

```text
R-DP-001 SELECTS R-ROUTE-D / Dedekind          UNCHANGED
R-BLOCK-001 accepted export                    UNCHANGED
R-ROUTE-C / Cauchy verified alternative        UNCHANGED / NOT SELECTED
C-DP-001 SELECTS C-ROUTE-P                     UNCHANGED
C-BLOCK-001                                    UNCHANGED
C-J-001 accepted integration Junction          UNCHANGED
C-BLOCK-002 / CA-20                            UNCHANGED
H6 provenance                                  UNCHANGED / NOT ACCEPTED C
```

A shared generic interface is compatibility evidence, not provenance identity
and not a reconvergence Junction by itself.

## 8. Lifecycle disposition

```text
ST2-EXP-011 MATHEMATICAL RESULT:              PASS
ST2-EXP-011 LIFECYCLE:                        CLOSED
GENERIC COMPARISON LAYER:                     VERIFIED RESEARCH EVIDENCE
ACCEPTED RBOMA INSTANCE:                      VERIFIED
NATIVE RCBOMA/H6 INSTANCE:                    VERIFIED
H5/DEDEKIND NATIVE IMPLEMENTATION DEPENDENCY: NOT REQUIRED
RELATION/FUNCTION FIREWALL:                   PRESERVED
ACCEPTED R/C:                                 UNCHANGED
SELECTED ROUTES:                              UNCHANGED
NEW BLOCK / DECISION / JUNCTION:              NO
DURABLE ARCHITECTURE KNOWLEDGE:               YES
CANONICAL REFACTOR:                           CANDIDATE ONLY / NOT PERFORMED
AUTOMATIC ACCEPTANCE PROMOTION:               NO
ACTIVE STAGE-TWO EXPERIMENT:                  NONE
```

## 9. Mandatory post-closure sequence

Closure releases the single-active-experiment slot, but it does not authorize
starting ST2-EXP-004 immediately. The owner fixed the sequence as:

```text
ST2-EXP-011 CLOSED
  ↓
separate Learning-to-Construction integration of the durable 011 lesson
  ↓
main synchronized and verified
  ↓
re-read current main authorities
  ↓
ST2-EXP-004 / R-DP-003 with a new independent Frozen Plan and branch
```

`ST2-EXP-004` is therefore the owner-sequenced next experiment but remains
**NOT ACTIVE** at this closure point and has no Frozen Plan yet.

The separate architecture Act must distinguish:

```text
ST2-EXP-001 production R→C surface
  = sixteen mathematical ordered-field/witness properties

ST2-EXP-011 comparison scalar surface
  = zero/one/neg/add/mul + explicit quadratic-coordinate laws
```

and must preserve accepted exports, `SELECTS`, and historical provenance.
