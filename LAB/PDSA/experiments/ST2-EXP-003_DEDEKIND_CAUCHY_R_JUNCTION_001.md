# ST2-EXP-003-R-J-001 — Dedekind/Cauchy Research R Reconvergence

**Scope:** research-only Stage-Two real-route reconvergence; not canonical accepted R, not a replacement `R-BLOCK-001`, and not an amendment to the Frozen Plan.  
**Study/Act decision:** `RESEARCH R JUNCTION: CREATE`  
**Gate status:** `EXACT PASS`  
**Exact verified head:** `7604590e73425b68de5500042d57f0a1c3abcde3`  
**Exact V5 run:** `32718925270`  
**Job:** `97405995050`  
**Artifact:** `9516996491`  
**Artifact SHA-256:** `0bec7afedf24e52cf8fd4ea04360b42f42c689e28d2fdfb54c52217f0f5f43e0`

## Incoming completed producers

1. Accepted `R-BLOCK-001 / R-ROUTE-D`: Dedekind `RBOMA`, whose accepted status and producers remain unchanged.
2. Experimental independent Cauchy `RCBOMA`, completed through arbitrary-family LUB at H4 with its selected-Dedekind-free audited closure.

The Cauchy route was completed and exact-verified before any H5 Dedekind comparison source was admitted to its independent manifest.

## Verified comparison inherited by the Junction

The Junction consumes the exact H5 root:

```text
BOMA.R.StageTwo.DedekindCauchyComparison003
  .dedekindCauchyFieldIsomorphismCertificate
```

with explicit maps:

```text
Cauchy -> Dedekind : cToD
Dedekind -> Cauchy : dToC
```

Both compositions are identities. The maps preserve the accepted rational embedding, order, addition, zero, negation, multiplication, one, nonzeroness, and the witness-only nonzero inverse relation.

The field-isomorphism source was independently exact-verified before this Junction at commit `2a875cdb93ed5b62fe1cc54a189b4c7d2d46eb0b`, run `32718253596`.

## Verified Junction declaration

```text
BOMA.R.StageTwo.DedekindCauchyJunction003
  .dedekindCauchyResearchJunctionCertificate
```

Its three packaged components are:

```text
acceptedDedekindRoute  : RStageIntegrationCertificate
independentCauchyRoute : CauchyLUBCertificate
fieldIsomorphism       : DedekindCauchyFieldIsomorphismCertificate
```

The Junction declares no new real carrier and introduces no new inverse selector or `Classical.choose`. It is an auditable reconvergence root over two already completed producers.

## Verification and logical surface

The exact-head V5 gate requires:

- accepted baseline ancestry;
- immutable Frozen Plan ancestry/content;
- H4 exact-verified ancestry and governance PASS;
- exact H5 field-isomorphism ancestry;
- post-H5 Study/Act ancestry and explicit `CREATE` decision;
- accepted Q/R/C manifests and accepted R producer unchanged;
- Junction and all H5 sources absent from the independent Cauchy manifest;
- no `sorry` or new source `axiom`;
- no new Junction-level Choice/inverse selector;
- Lean typechecking;
- `#print axioms` for both field-isomorphism and Junction roots;
- dependency closure rooted at the Junction certificate;
- artifact preservation.

Lean reports for the Junction root exactly:

```text
[propext, Classical.choice, Quot.sound]
```

This is the same inherited kernel surface as H4 and final H5.

## Preserved distinction

The Junction proves field/order reconvergence, not provenance identity:

```text
same accepted QBOMA boundary
same ordered-field/completeness strength
explicit field/order isomorphism
!= same carrier
!= same construction history
!= same source-level commitments
!= same producer cost
```

Dedekind remains the accepted real realization. Cauchy remains research-only.

## Downstream authorization boundary

The Frozen Plan's affected cone includes the selected quadratic C construction. The exact Junction PASS now authorizes a **branch-only** downstream C rebuild over the alternative Cauchy real interface.

That rebuild must not modify accepted C manifests/producers, must remain research-only, and must preserve the accepted quadratic algebraic contract while changing only the real-interface producer. Any later comparison with accepted C must occur after the alternative C build is independently closed.

`main`, accepted R, and accepted C remain unchanged.
