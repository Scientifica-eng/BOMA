# R DAG — Accepted Dedekind Spine + Permanent Verified Cauchy Alternative

**Status:** **R ACCEPTED / RA-22 / R-BLOCK-001 + VERIFIED ALTERNATIVE BRANCH INTEGRATED**  
**Date:** 2026-08-24  
**Input:** `Q-BLOCK-002 — Accepted Stage-One Rational Interface`  
**Decision Point:** `R-DP-001 — Dedekind selected`  
**Accepted export:** `R-BLOCK-001`  
**Accepted integration:** `R-J-002`  
**Permanent verified alternative:** `R-ROUTE-C / Cauchy`  
**Alternative reconvergence:** `ST2-EXP-003-R-J-001`  
**Stage-Two lifecycle:** `ST2-EXP-003 CLOSED / PASS / NO ACTIVE EXPERIMENT`  
**Integration authority:** `BOMA-ST2-LEARNING-INTEGRATION-001`.

## 1. Current R construction DAG

```text
Q-BLOCK-002 — accepted Q
   ↓
R-DP-001 — real completion route / RESOLVED
   ├── SELECTS ──► R-ROUTE-D / Dedekind
   │                 ↓
   │             LowerCut / CutEquiv
   │                 ↓
   │             R-DP-002 — quotient identity selected
   │                 ↓
   │             RBOMA := Quotient cutSetoid
   │                 ↓
   │             rOfQ + constructive rLE core
   │                 ↓
   │             R-DP-003 — localized total-comparability witness
   │                 ↓
   │             R-COMP-BLOCK-001 — Dedekind LUB
   │                 ↓
   │             R-ADD-GROUP-BLOCK-001
   │                 ↓
   │             R-DP-004 / R-DP-005
   │                 ↓
   │             R-MUL-BLOCK-001
   │                 ↓
   │             R-DP-006 — direct Dedekind reciprocal
   │                 ↓
   │             R-FIELD-BLOCK-001
   │                 ↓
   │             R-DENSITY-BLOCK-001 + R-ARCH-BLOCK-001
   │                 ↓
   │             R-J-002 — SAME-CARRIER-INTEGRATION PASS
   │                 ↓
   │             RA-22 ACCEPT
   │                 ↓
   │             R-BLOCK-001 — ACCEPTED R
   │
   └── BRANCHES_TO / ALTERNATIVE_TO ──► R-ROUTE-C / Cauchy
                                           ↓
                                    independent Cauchy quotient
                                           ↓
                                    ordered field + sequential completeness
                                           ↓
                                    exact LUB completeness
                                           ↓
                                    VERIFIED ALTERNATIVE R PRODUCER
```

The Cauchy branch is now a **permanent verified alternative branch in the R
Construction DAG**. It is not merely an experiment appendix, but it remains
non-selected and is not an accepted export.

## 2. Verified Dedekind/Cauchy reconvergence

After the Cauchy producer was completed independently, comparison was admitted:

```text
R-BLOCK-001 / Dedekind producer ───────┐
                                       │
                                       ▼
                              ST2-EXP-003-R-J-001
                              R-FIELD-ISOMORPHISM
                                       ▲
                                       │
R-ROUTE-C / completed Cauchy producer ─┘
```

The Junction certifies mutually inverse field/order maps preserving the rational
embedding and inverse-witness relations required by the experiment.

It is a **permanent verified alternative-construction Junction** with research
provenance. It does not replace `R-J-002`, which remains the accepted
same-carrier integration Junction for the selected Dedekind construction.

Exact theorem root:

`BOMA.R.StageTwo.DedekindCauchyJunction003.dedekindCauchyResearchJunctionCertificate`.

## 3. Selected Dedekind realization decisions

```text
R-DP-001  Dedekind selected; Cauchy permanent verified alternative
R-DP-002  quotient CutEquiv identity selected
R-DP-003  localized classical CutComparability selected
R-DP-004  reusable Q Archimedean approximation route selected
R-DP-005  positive/negative-part multiplication selected
R-DP-006  direct positive Dedekind reciprocal selected
```

`SELECTS` remains a project/methodological choice, not a necessity theorem.

## 4. Accepted R meaning

`R-BLOCK-001` remains the accepted R export and includes the accepted real Claim
surface: faithful/order-exact Q embedding, ordered-field behavior at
witness-only inverse scope, LUB completeness, density, and Archimedean
characterization.

The inverse interface remains witness-based; no global Choice-backed inverse
selector is required by acceptance.

The selected Dedekind implementation of these meanings is not made necessary by
its acceptance. `ST2-EXP-003` provides explicit evidence that a distinct Cauchy
producer can recover the relevant ordered-field/completeness meaning.

## 5. Downstream C evidence from the alternative producer

H6 rebuilt natively over the Cauchy real producer seven selected
`C-BLOCK-001` core meanings:

```text
C-CL-CARRIER-001
C-CL-REMBED-001
C-CL-FIELD-001
C-CL-I-001
C-CL-GEN-001
C-CL-COORDUNIQ-001
C-CL-NONREAL-001
```

This is permanent downstream robustness evidence attached to the alternative R
branch. It does not make H6 C an accepted C producer and does not change
`C-DP-001`.

## 6. Provenance

`ST2-EXP-003` remains a closed PDSA experiment whose history is preserved:

```text
Frozen Plan
H1-H4 independent construction evidence
H5 field-isomorphism evidence
ST2-EXP-003-R-J-001 evidence
H6 Cauchy-native C evidence
failed runs and prevention lessons
Final Study/Act
lifecycle closure
```

Final mathematical pre-merge exact head:

`3d4ed58e5d88b2a0bd84b3958cac2c8572385152`.

Final critical experiment runs:

```text
Independent Cauchy Real Route               32727267231 PASS
Dedekind-Cauchy Comparison / Research R J   32727267177 PASS
Cauchy Quadratic Rebuild                     32727267183 PASS
```

Lifecycle closure and later architecture integration do not rewrite this history.

## 7. Current R boundary

```text
R-DP-001 selected route              R-ROUTE-D / Dedekind
accepted R integration               R-J-002
accepted R export                    R-BLOCK-001
permanent verified alternative       R-ROUTE-C / Cauchy
alternative-construction Junction    ST2-EXP-003-R-J-001
H6 downstream evidence               retained / non-accepted
RE-R-001                             CLOSED / COMPLETE
ST2-EXP-003                          CLOSED / PASS / lesson integrated
NO ACTIVE EXPERIMENT
```

Future R-related candidates remain proposals until separately owner-selected.
