# R DAG — Accepted Stage-One Real Construction

**Status:** **STAGE-ONE R ACCEPTED — RA-22 ACCEPT; RE-R-001 CLOSED**  
**Input:** `Q-BLOCK-002 — Accepted Stage-One Rational Interface`  
**Governing specification:** `BOMA-R-ACCEPT-001`  
**Accepted export:** `R-BLOCK-001`  
**Reverse gate:** `RE-R-001 COMPLETE / PDSA-R-015`  
**Downstream accepted family:** `C-BLOCK-002 / CA-20 ACCEPT`  
**Stage-Two real frontier:** `ST2-EXP-003 CLOSED / PASS / CAUCHY VERIFIED RESEARCH / NO ACTIVE EXPERIMENT`

## Canonical forward spine

```text
Q-BLOCK-002 — accepted Q
   ↓
shared Q order / density / approximation gateways
   ↓
R-DP-001
   ├── Dedekind lower cuts SELECTED for accepted Stage One
   └── Cauchy completion RETAINED / VERIFIED RESEARCH
         ST2-EXP-003 CLOSED / PASS
         research Junction ST2-EXP-003-DEDEKIND-CAUCHY-R-JUNCTION-001
   ↓
LowerCut / CutEquiv
   ↓
R-DP-002 — quotient identity selected
   ↓
RBOMA := Quotient cutSetoid
   ↓
rOfQ + rLE
   ├── constructive partial-order core
   └── R-DP-003 localized classical total-comparability witness
   ↓
R-COMP-BLOCK-001 — Dedekind LUB completeness
   ↓
R-ADD-GROUP-BLOCK-001 — additive group
   ↓
R-DP-004 — reusable Q Archimedean approximation route
   ↓
R-DP-005 — positive/negative decomposition multiplication
   ↓
R-MUL-BLOCK-001 — ordered commutative ring
   ↓
R-DP-006 — direct Dedekind reciprocal route selected
   ↓
R-FIELD-BLOCK-001 — unique nonzero inverse witnesses
   ↓
R-DENSITY-BLOCK-001 + R-ARCH-BLOCK-001
   ↓
explicit ordered-field closure
   ↓
R-J-002 — acceptance-strength integration PASS
   ↓
PDSA-R-014
   ↓
RA-22 ACCEPT
   ↓
R-BLOCK-001 — accepted Stage-One R
   ↓
PDSA-R-015 / RE-R-001
   ↓
reverse classification COMPLETE
```

## Representation / identity

Selected raw representation:

```text
LowerCut.lower : QBOMA → Prop
nonempty
proper
downward
rounded
```

Identity:

```text
CutEquiv A B := ∀q, A.lower q ↔ B.lower q
RBOMA := Quotient cutSetoid
```

`R-DP-002` remains a formalization/methodological choice. `RE-R-001` confirms
that explicit identity is acceptance-required while the selected quotient
realization is not a universal mathematical necessity claim.

## Rational embedding and order

```text
rOfQ : QBOMA → RBOMA
```

is injective and exactly order-preserving/reflecting. Separate certified paths
preserve Q zero, one, negation, addition, multiplication, and the positive
inverse relation.

The selected non-strict order has a constructive reflexive/transitive/
antisymmetric core. `R-DP-003` supplies total cut comparability through a
localized classical witness consumed through a narrow totality interface.

`RE-R-001` classifies this as a **LOGICAL COMMITMENT / branch candidate**, not
as part of the definition of the real carrier.

## Accepted completeness and field package

`R-COMP-BLOCK-001` proves Dedekind least-upper-bound completeness for nonempty
bounded predicates on `RBOMA`. Its property is acceptance-required; the
union-of-lower-regions realization is Dedekind-route-specific.

The accepted package also includes:

```text
0_R ≠ 1_R
addition translation invariance
negation order reversal
nonnegative-factor multiplication monotonicity
positive inverse behavior
strict Q-image density
BOMA-natural Archimedean upper-bound characterization
unique nonzero inverse witnesses
```

The accepted inverse interface remains witness-based; no global Choice-backed
inverse selector is required by acceptance.

## Selected construction decisions

```text
R-DP-001  Dedekind lower cuts selected; Cauchy retained as verified research
R-DP-002  quotient CutEquiv identity selected
R-DP-003  localized classical CutComparability selected
R-DP-004  reusable Q Archimedean approximation route selected
R-DP-005  positive/negative-part multiplication selected
R-DP-006  direct positive Dedekind reciprocal selected
```

These are accepted realization choices, not universal necessity theorems.

## Acceptance closure

```text
R-J-002  PASS / RESOLVED
RA-22    ACCEPT
R-BLOCK-001 ACCEPTED
```

Final accepted integration evidence remains run `32374868448`, verified source
`f07363c22b049a3fae028a927df74d4fb28a0680`, under Lean `4.32.1`.

Canonical records:

```text
LAB/PDSA/PDSA-R-014_STAGE_ONE_REAL_CLOSURE.md
LAB/20_FORMALIZATION/R_STAGE/R_STAGE_ACCEPTANCE_CLOSURE_AUDIT_002.md
```

## Post-R reverse engineering — CLOSED / COMPLETE

`PDSA-R-015 / RE-R-001` separates:

```text
acceptance-required interface properties
selected Dedekind route dependencies
formal identity choices
localized logical commitments
reusable carrier-independent Q contributions
verification/proof-engineering infrastructure
retained alternative branches
```

Principal classifications:

```text
Dedekind route             ROUTE-SPECIFIC — not universal necessity
Quotient cut identity      FORMALIZATION / METHODOLOGICAL CHOICE
Classical CutComparability LOGICAL COMMITMENT / replaceable branch route
LUB property               acceptance-required; union proof Dedekind-specific
Q approximation machinery  reusable certified contribution
multiplication architecture selected / branch candidate
direct reciprocal          Dedekind-specific / branch candidate
RA-12 density property     acceptance-required; current proof route-specific
RA-13 Archimedean property acceptance-required; current proof route-specific
```

Canonical reverse matrix:

`LAB/PDSA/experiments/PDSA-R-015-RE-R-001-DEPENDENCY-CLASSIFICATION.md`.

No accepted R theorem was weakened or revoked by reverse engineering.

## ST2-EXP-003 — retained independent Cauchy route, now CLOSED

Architectural origin:

```text
Q-BLOCK-002
   ↓
R-DP-001
   ├── accepted R-ROUTE-D / Dedekind / R-BLOCK-001
   └── research R-ROUTE-C / Cauchy
          ↓
      independently completed ordered field + LUB
          ↓
      H5 Dedekind/Cauchy field isomorphism
          ↓
      ST2-EXP-003-DEDEKIND-CAUCHY-R-JUNCTION-001
          ↓
      H6 Cauchy-native rebuild of seven selected C-BLOCK-001 core meanings
```

Final experiment state:

```text
ST2-EXP-003 = CLOSED / PASS / H1-H6 EXACT PASS
canonical R = Dedekind retained
accepted R-BLOCK-001 = unchanged
research Cauchy R = retained / not accepted
H6 C = retained research evidence / not accepted
NO ACTIVE EXPERIMENT
```

Final exact-verified pre-merge head:

`3d4ed58e5d88b2a0bd84b3958cac2c8572385152`.

Critical final runs:

```text
Independent Cauchy Real Route               32727267231 PASS
Dedekind-Cauchy Comparison / Research R J   32727267177 PASS
Cauchy Quadratic Rebuild                     32727267183 PASS
```

PR #8 merged the exact-verified tree to `main` as
`5431ac81e7327f5bf4b06b3ab7fdb2bcb5b69efd`. Lifecycle closure was then
explicitly authorized on 2026-08-24.

Closure authority:

`LAB/PDSA/experiments/ST2-EXP-003_LIFECYCLE_CLOSURE_001.md`.

The research result demonstrates representation robustness of the selected
real-field meaning. It does not replace accepted Dedekind R and does not reduce
the final kernel axiom surface below `[propext, Classical.choice, Quot.sound]`.

## Current R/Stage-Two frontier

```text
R-BLOCK-001           ACCEPTED / DEDEKIND
RE-R-001              CLOSED / COMPLETE
ST2-EXP-001           CLOSED / PASS
ST2-EXP-002           CLOSED / PASS
ST2-EXP-003           CLOSED / PASS
NO ACTIVE EXPERIMENT
NEXT EXPERIMENT SLOT  OPEN / OWNER SELECTION REQUIRED
```

Remaining R-related candidates in the controlled register include alternatives
at `R-DP-003`, `R-DP-002`, and `R-DP-006`. None is authorized merely by the
closure of ST2-EXP-003.