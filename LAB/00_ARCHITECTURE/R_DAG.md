# R DAG — Accepted Dedekind Spine + Permanent Verified Cauchy Alternative + R-DP-003 Logical-Regime Boundary

**Status:** **R ACCEPTED / RA-22 / R-BLOCK-001 + VERIFIED ALTERNATIVE BRANCH + ST2-EXP-004 LOGICAL-REGIME KNOWLEDGE INTEGRATED**  
**Date:** 2026-08-25  
**Input:** `Q-BLOCK-002 — Accepted Stage-One Rational Interface`  
**Decision Point:** `R-DP-001 — Dedekind selected`  
**Logical-regime Decision Point:** `R-DP-003 — localized classical comparability selected`  
**Accepted export:** `R-BLOCK-001`  
**Accepted integration:** `R-J-002`  
**Permanent verified alternative:** `R-ROUTE-C / Cauchy`  
**Alternative reconvergence:** `ST2-EXP-003-R-J-001`  
**Stage-Two lifecycle:** `ST2-EXP-003 + ST2-EXP-004 CLOSED / PASS / LESSONS INTEGRATED / NO ACTIVE EXPERIMENT`  
**Integration authorities:** `BOMA-ST2-LEARNING-INTEGRATION-001`, `BOMA-ST2-LEARNING-INTEGRATION-003`.

## 1. Current R construction DAG

```text
Q-BLOCK-002 — accepted Q
   ↓
R-DP-001 — real completion route / RESOLVED
   ├── SELECTS ──► R-ROUTE-D / Dedekind
   │                 ↓
   │             LowerCut / CutEquiv
   │                 ↓
   │             R-DP-002 — quotient identity selected
   │                 ↓
   │             RBOMA := Quotient cutSetoid
   │                 ↓
   │             rOfQ + constructive rLE core
   │                 ↓
   │             R-DP-003 — localized classical CutComparability selected
   │                 │     integrated boundary: RTotality ↔ CutComparability
   │                 ↓
   │             R-COMP-BLOCK-001 — Dedekind LUB
   │                 ↓
   │             R-ADD-GROUP-BLOCK-001
   │                 ↓
   │             R-DP-004 / R-DP-005
   │                 ↓
   │             R-MUL-BLOCK-001
   │                 ↓
   │             R-DP-006 — direct Dedekind reciprocal
   │                 ↓
   │             R-FIELD-BLOCK-001
   │                 ↓
   │             R-DENSITY-BLOCK-001 + R-ARCH-BLOCK-001
   │                 ↓
   │             R-J-002 — SAME-CARRIER-INTEGRATION PASS
   │                 ↓
   │             RA-22 ACCEPT
   │                 ↓
   │             R-BLOCK-001 — ACCEPTED R
   │
   └── BRANCHES_TO / ALTERNATIVE_TO ──► R-ROUTE-C / Cauchy
                                           ↓
                                    independent Cauchy quotient
                                           ↓
                                    ordered field + sequential completeness
                                           ↓
                                    exact LUB completeness
                                           ↓
                                    VERIFIED ALTERNATIVE R PRODUCER
```

The Cauchy branch is a **permanent verified alternative branch in the R Construction DAG**. It remains non-selected and is not an accepted export.

## 2. Verified Dedekind/Cauchy reconvergence

After the Cauchy producer was completed independently, comparison was admitted:

```text
R-BLOCK-001 / Dedekind producer ───────┐
                                       │
                                       ▼
                              ST2-EXP-003-R-J-001
                              R-FIELD-ISOMORPHISM
                                       ▲
                                       │
R-ROUTE-C / completed Cauchy producer ─┘
```

The Junction certifies mutually inverse field/order maps preserving the rational embedding and inverse-witness relations required by the experiment.

It is a **permanent verified alternative-construction Junction** with research provenance. It does not replace `R-J-002`, which remains the accepted same-carrier integration Junction for the selected Dedekind construction.

Exact theorem root:

`BOMA.R.StageTwo.DedekindCauchyJunction003.dedekindCauchyResearchJunctionCertificate`.

## 3. Selected Dedekind realization decisions

```text
R-DP-001  Dedekind selected; Cauchy permanent verified alternative
R-DP-002  quotient CutEquiv identity selected
R-DP-003  localized classical CutComparability selected; ST2-EXP-004 impact boundary integrated
R-DP-004  reusable Q Archimedean approximation route selected
R-DP-005  positive/negative-part multiplication selected
R-DP-006  direct positive Dedekind reciprocal selected
```

`SELECTS` remains a project/methodological choice, not a necessity theorem.

## 4. ST2-EXP-004 integrated logical-regime knowledge

The selected `R-DP-003` Stage-I regime remains:

```text
constructive partial-order core
+
localized classical F-04 witness of CutComparability
+
constructive totality-from-CutComparability bridge
```

`ST2-EXP-004` held the accepted carrier, identity, `rLE`, operation definitions, accepted manifests, and F-05/F-06/F-07 fixed while removing only the selected F-04 provider in research assemblies.

On the unchanged accepted representation, the experiment proved:

```text
RTotality ↔ CutComparability
```

where `RTotality := ∀ x y : RBOMA, rLE x y ∨ rLE y x`.

No unconditional constructive `CutComparability` inhabitant was recovered from the frozen `LowerCut` interface. Therefore a conditional theorem from an explicit comparability hypothesis is not a constructive recovery, and the experiment did not make accepted R constructive.

Gate-A current-declaration impact:

```text
F04_DIRECT             8
F04_TRANSITIVE         7
F04_FREE              22
OTHER_CLASSICAL_ONLY  18
unresolved             0
internal axioms        0
```

Gate B retained `77 / 88` accepted-manifest R files in the research-only whole-source no-F04 survivor assembly. That is a source-packaging measurement, not an impossibility theorem.

Independent logical controls remain:

```text
F-05 finite membership / cut bracketing
F-06 positive representative extraction
F-07 rational density
```

Permanent interpretation:

```text
declaration dependency ≠ whole-source elaboration dependency
formal provenance       ≠ mathematical necessity
NO F-04 dependency      ≠ fully constructive R
```

A located-cut redesign would change the representation and belongs only to a separately authorized future candidate.

## 5. Downstream sensitivity of the C production boundary

`ST2-EXP-004` also measured the effect of removing only `orderTotal` from the exact sixteen-property C-production surface learned in `ST2-EXP-001`.

Seven accepted C Claim families survived:

```text
C-CL-CARRIER-001
C-CL-REMBED-001
C-CL-I-001
C-CL-GEN-001
C-CL-COORDUNIQ-001
C-CL-NONREAL-001
C-CL-COMPARE-001
```

The current proof closures for `C-CL-FIELD-001` and `C-CL-INTEGRATION-001` did not survive; their measured dependence is transitive through the current square/nonnegative → norm → field route. This is current proof-architecture sensitivity, not a mathematical-necessity theorem.

`C-CL-COMPARE-001` survives consistently with the independently integrated `ST2-EXP-011` comparison interface.

## 6. Accepted R meaning

`R-BLOCK-001` remains the accepted R export and includes the accepted real Claim surface: faithful/order-exact Q embedding, ordered-field behavior at witness-only inverse scope, LUB completeness, density, and Archimedean characterization.

The inverse interface remains witness-based; no global Choice-backed inverse selector is required by acceptance.

The selected Dedekind implementation of these meanings is not made necessary by its acceptance. `ST2-EXP-003` provides explicit evidence that a distinct Cauchy producer can recover the relevant ordered-field/completeness meaning.

`ST2-EXP-004` changes none of those accepted meanings or producers; it refines the logical-cost and sensitivity knowledge attached to the selected Dedekind route.

## 7. Downstream C evidence from the alternative producer

H6 rebuilt natively over the Cauchy real producer seven selected `C-BLOCK-001` core meanings:

```text
C-CL-CARRIER-001
C-CL-REMBED-001
C-CL-FIELD-001
C-CL-I-001
C-CL-GEN-001
C-CL-COORDUNIQ-001
C-CL-NONREAL-001
```

This is permanent downstream robustness evidence attached to the alternative R branch. It does not make H6 C an accepted C producer and does not change `C-DP-001`.

## 8. Provenance

`ST2-EXP-003` remains a closed PDSA experiment whose history is preserved. Its final mathematical pre-merge exact head is `3d4ed58e5d88b2a0bd84b3958cac2c8572385152`.

`ST2-EXP-004` remains a closed PDSA experiment whose Frozen Plan, Gate A-F records, thirteen failure records, Final Study/Act, lifecycle closure, and read-only exact-head evidence remain immutable Learning-Graph provenance.

ST2-EXP-004 exact research merge:

```text
research head  1fe760de811ad2b176ead6f420b80ca1aab5ce46
merge commit   61adb8589c803e95e1b96ef38902320c8aa5df19
merge tree     256b4dce1bdbb93858b28d28ece7a9e177a1685c
content drift  zero files
```

Integration authority:

`LAB/PDSA/STAGE_TWO_SUCCESSFUL_EXPERIMENTS_ARCHITECTURE_INTEGRATION_003.md`.

## 9. Current R boundary

```text
R-DP-001 selected route              R-ROUTE-D / Dedekind
R-DP-003 selected logical regime     localized classical comparability / retained
R-DP-003 learned boundary            RTotality ↔ CutComparability / integrated
accepted R integration               R-J-002
accepted R export                    R-BLOCK-001
permanent verified alternative       R-ROUTE-C / Cauchy
alternative-construction Junction    ST2-EXP-003-R-J-001
H6 downstream evidence               retained / non-accepted
RE-R-001                             CLOSED / COMPLETE
ST2-EXP-003                          CLOSED / PASS / lesson integrated
ST2-EXP-004                          CLOSED / PASS / lesson integrated
ACTIVE EXPERIMENT                    NONE
NEXT EXPERIMENT                      NOT AUTHORIZED
REQUIRED NEXT ACT                    STOP / OWNER AUTHORIZATION REQUIRED
```

No new Block, Decision Point, Junction, accepted source, accepted manifest, selected route, or accepted export is created by the ST2-EXP-004 learning integration.