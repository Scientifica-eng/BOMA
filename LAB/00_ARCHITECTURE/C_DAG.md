# C DAG — Accepted Route P + Permanent Verified Alternative Branches

**Status:** **ACCEPTED — CA-20 / C-ROUTE-P SELECTED / VERIFIED ALTERNATIVES INTEGRATED**  
**Date:** 2026-08-24  
**Selected upstream R export:** `R-BLOCK-001`  
**Canonical mathematical R→C contract:** `BOMA-C-R-DEP-001 — exact sixteen-property interface`  
**Decision Point:** `C-DP-001 RESOLVED / USER-RATIFIED C-ROUTE-P`  
**Selected producer:** `C-BLOCK-001`  
**Accepted integration:** `C-J-001`  
**Accepted export:** `C-BLOCK-002 / CA-20 ACCEPT`  
**Permanent verified C alternative:** `C-ROUTE-Q`  
**Alternative reconvergence:** `ST2-EXP-002-PQ-J-001`  
**Stage-Two lifecycle:** `ST2-EXP-001..003 CLOSED / PASS / NO ACTIVE EXPERIMENT`  
**Integration authority:** `BOMA-ST2-LEARNING-INTEGRATION-001`.

## 1. Current C construction DAG

```text
R-DP-001
   └── SELECTS R-ROUTE-D / Dedekind
            ↓
       R-BLOCK-001 — accepted R export
            ↓
       BOMA-C-R-DEP-001
       exact sixteen-property mathematical interface
       [ST2-EXP-001 lesson integrated]
            ↓
       BOMA-C-ACCEPT-001
       representation-neutral C acceptance semantics
            ↓
       C-DP-001 — RESOLVED
          ├── SELECTS ──► C-ROUTE-P / ordered-pair rank-two route
          │                 ↓
          │             C-BLOCK-001 — selected witness-field producer
          │                 ↓
          │             C-COMPARE-BLOCK-001
          │                 ↓
          │             C-J-001 — SAME-CARRIER-INTEGRATION PASS
          │                 ↓
          │             C-BLOCK-002 / CA-20 ACCEPT
          │
          └── BRANCHES_TO / ALTERNATIVE_TO ──► C-ROUTE-Q
                                                 ↓
                                      independent expression-normalization
                                      quotient field producer
                                                 ↓
                                      VERIFIED ALTERNATIVE C PRODUCER
```

The accepted path and the permanent verified alternative coexist in the same
Construction DAG. `SELECTS C-ROUTE-P` remains unchanged.

## 2. Canonical R→C mathematical dependency boundary — ST2-EXP-001 integrated

The exact mathematical properties directly consumed by the selected C meaning
are:

```text
orderTrans
orderAntisymm
orderTotal
nontrivial
addComm
addAssoc
addZeroLeft
addInverseRight
addTranslateOrderIff
negOrderReversing
mulComm
mulAssoc
mulOneLeft
distribRight
orderMulNonneg
inverseExists
```

`ST2-EXP-001` rebuilt the selected C adequacy over this narrower package and
closed PASS. Therefore this is now the canonical mathematical surface of
`BOMA-C-R-DEP-001`.

This does **not** claim that the existing accepted C Lean assembly has already
shed every inherited bundled R declaration. The larger existing formal ancestry
is classified as formalization/provenance over-bundling, not mathematical
necessity.

Formal narrow-interface witness:

```text
BOMA.C.StageTwo.NarrowRInterface001.NarrowROrderedFieldCertificate
BOMA.C.StageTwo.NarrowRInterface001.narrowRFromAcceptedProducers
```

## 3. Permanent Route-Q branch — ST2-EXP-002 integrated

The non-selected Route Q is a completed independent field producer, not merely a
future candidate:

```text
C-DP-001
   ├── C-ROUTE-P / selected producer ─────┐
   │                                      │
   │                                      ▼
   │                            ST2-EXP-002-PQ-J-001
   │                            R-FIELD-ISOMORPHISM
   │                                      ▲
   │                                      │
   └── C-ROUTE-Q / verified alternative ──┘
```

`ST2-EXP-002-PQ-J-001` preserves the embedded accepted R and distinguished I.
It is a **permanent verified alternative-construction Junction**, not an
acceptance Junction and not a replacement for `C-J-001`.

Current route status:

```text
C-ROUTE-P   SELECTED / accepted producer path
C-ROUTE-Q   PERMANENT VERIFIED ALTERNATIVE / NON-SELECTED / NOT ACCEPTED EXPORT
C-ROUTE-A   conditional slot only if a genuinely independent future route exists
```

## 4. Accepted C meaning

The accepted C semantics remain:

```text
faithful accepted-R embedding
commutative-field-strength behavior
distinguished I
I² = -1
every element has an a+bI expression
that expression is unique
non-real-collapse consequence
representation-neutral comparison adequacy
same-carrier integration
```

No algebraic closure, FTA, analytic completeness, total order on C, or built-in
Complex identity is silently imported by acceptance.

Accepted ownership remains:

```text
C-BLOCK-001          selected seven-core-Claim producer
C-COMPARE-BLOCK-001  C-CL-COMPARE-001
C-J-001              C-CL-INTEGRATION-001
C-BLOCK-002          final accepted export
CA-20                ACCEPT
```

## 5. ST2-EXP-003 downstream robustness evidence

The independently completed Cauchy real branch originates at `R-DP-001` and
reconverges with selected Dedekind R through `ST2-EXP-003-R-J-001`.

H6 then rebuilt natively over the Cauchy producer these seven selected
`C-BLOCK-001` core meanings:

```text
C-CL-CARRIER-001
C-CL-REMBED-001
C-CL-FIELD-001
C-CL-I-001
C-CL-GEN-001
C-CL-COORDUNIQ-001
C-CL-NONREAL-001
```

This evidence is permanently attached to the C DAG as downstream
representation-robustness evidence, but H6 C remains non-accepted.

The comparison/integration layer remains scalar-hard-wired to the selected
`RBOMA / RStageIntegrationCertificate` realization. Generalizing that boundary
is the separately documented `ST2-EXP-011` candidate and is **not** authorized
by this integration Act.

## 6. Provenance

The Construction DAG now contains permanent verified alternatives, while the
Learning Graph preserves how they were learned:

```text
ST2-EXP-001   CLOSED / PASS — exact narrow R→C dependency lesson
ST2-EXP-002   CLOSED / PASS — independent Route-Q producer + P/Q Junction
ST2-EXP-003   CLOSED / PASS — Cauchy R + R Junction + H6 downstream rebuild
```

No Frozen Plan, failed run, Study/Act record, or exact evidence is erased.

## 7. Current C boundary

```text
selected upstream R export            R-BLOCK-001 / Dedekind
canonical R→C mathematical contract   exact sixteen-property surface
C-DP-001                               RESOLVED / SELECTS C-ROUTE-P
selected C producer                    C-BLOCK-001
accepted C integration                 C-J-001
accepted C export                      C-BLOCK-002 / CA-20
permanent C alternative                C-ROUTE-Q
alternative-construction Junction      ST2-EXP-002-PQ-J-001
Cauchy-native H6 evidence              retained / non-accepted
ST2-EXP-011                            candidate / not authorized
NO ACTIVE EXPERIMENT
```
