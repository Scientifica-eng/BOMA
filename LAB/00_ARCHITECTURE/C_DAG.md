# C DAG — Accepted Route P + Permanent Verified Alternatives + Generic Comparison Boundary

**Status:** **ACCEPTED — CA-20 / C-ROUTE-P SELECTED / STAGE-TWO VERIFIED LESSONS INTEGRATED**  
**Date:** 2026-08-24  
**Selected upstream R export:** `R-BLOCK-001`  
**Canonical production R→C contract:** `BOMA-C-R-DEP-001 — exact sixteen-property interface`  
**Canonical comparison scalar boundary:** `C-COMPARE-BLOCK-001 — zero/one/neg/add/mul + explicit quadratic coordinate laws`  
**Decision Point:** `C-DP-001 RESOLVED / USER-RATIFIED C-ROUTE-P`  
**Selected producer:** `C-BLOCK-001`  
**Accepted comparison Claim owner:** `C-COMPARE-BLOCK-001 / C-CL-COMPARE-001`  
**Accepted integration:** `C-J-001`  
**Accepted export:** `C-BLOCK-002 / CA-20 ACCEPT`  
**Permanent verified C alternative:** `C-ROUTE-Q`  
**Alternative reconvergence:** `ST2-EXP-002-PQ-J-001`  
**Stage-Two lifecycle:** `ST2-EXP-001..003 + ST2-EXP-011 CLOSED / PASS / NO ACTIVE EXPERIMENT`  
**Integration authorities:** `BOMA-ST2-LEARNING-INTEGRATION-001` and `BOMA-ST2-LEARNING-INTEGRATION-002`.

## 1. Current C construction DAG

```text
R-DP-001
   ├── SELECTS R-ROUTE-D / Dedekind → R-BLOCK-001 ACCEPTED
   └── R-ROUTE-C / Cauchy → VERIFIED ALTERNATIVE R PRODUCER
                                ↓
                         ST2-EXP-003-R-J-001
                         R-FIELD-ISOMORPHISM / NON-ACCEPTANCE

R-BLOCK-001
   ↓
BOMA-C-R-DEP-001
exact sixteen-property PRODUCTION mathematical interface
[ST2-EXP-001 lesson integrated]
   ↓
BOMA-C-ACCEPT-001
   ↓
C-DP-001 — RESOLVED
   ├── SELECTS ──► C-ROUTE-P
   │                 ↓
   │             C-BLOCK-001 — selected witness-field producer
   │                 ↓
   │             C-COMPARE-BLOCK-001
   │             comparison scalar boundary:
   │             zero/one/neg/add/mul
   │             + explicit quadratic coordinate laws
   │             [ST2-EXP-011 lesson integrated]
   │                 ↓
   │             C-J-001 — SAME-CARRIER-INTEGRATION PASS
   │                 ↓
   │             C-BLOCK-002 / CA-20 ACCEPT
   │
   └── ALTERNATIVE_TO ──► C-ROUTE-Q
                            ↓
                      independent expression-normalization quotient field
                            ↓
                      VERIFIED ALTERNATIVE C PRODUCER
                            ↓
                      ST2-EXP-002-PQ-J-001
                      R-FIELD-ISOMORPHISM / NON-ACCEPTANCE
```

The accepted path, alternative routes, and generic comparison knowledge coexist
without changing either `R-DP-001 SELECTS` or `C-DP-001 SELECTS`.

## 2. Production R→C mathematical boundary — ST2-EXP-001

The exact mathematical properties required to produce the selected C meaning
remain:

```text
orderTrans
orderAntisymm
orderTotal
nontrivial
addComm
addAssoc
addZeroLeft
addInverseRight
addTranslateOrderIff
negOrderReversing
mulComm
mulAssoc
mulOneLeft
distribRight
orderMulNonneg
inverseExists
```

This is the canonical production surface of `BOMA-C-R-DEP-001`.

The existing accepted C Lean implementation may still carry a larger historical
R bundle. Such additional ancestry is formalization/provenance over-bundling,
not mathematical necessity.

## 3. Quadratic comparison boundary — ST2-EXP-011 integrated

`ST2-EXP-011` answered a narrower question than `ST2-EXP-001`: what does the
quadratic **comparison** machinery itself require from its scalar layer?

The direct scalar-operation dependency is:

```text
zero
one
neg
add
mul
```

plus explicit quadratic coordinate laws:

```text
coord
coordinateGeneration
coordinateUnique
coordinateZero
coordinateOne
coordinateReal
coordinateImag
coordinateNeg
coordinateAdd
coordinateMul
```

This surface belongs to `C-COMPARE-BLOCK-001 / C-CL-COMPARE-001`; it does not
replace `BOMA-C-R-DEP-001`.

Canonical distinction:

```text
C production dependency
  = 16-property ordered-field/witness surface

C comparison dependency
  = five scalar operations + explicit coordinate laws
```

No scalar order, completeness, density, Archimedean, general field-law, or
inverse-existence field is a direct comparison-proof requirement.

## 4. Accepted comparison semantics and implementation status

The ST2-EXP-011 accepted-RBOMA adapter verifies the generic relation has the
same shared-coordinate semantics as the accepted relation; `related_iff_accepted`
is definitionally proved by `rfl`.

Therefore the accepted Claim meaning is unchanged:

```text
C-CL-COMPARE-001 meaning              unchanged
accepted CQuadraticComparison001      unchanged
accepted C manifests                  unchanged
C-J-001                               unchanged
C-BLOCK-002 / CA-20                   unchanged
```

The experimental generic Lean layer remains research-only. The architecture now
records the smaller sufficient comparison contract, while a literal accepted
source refactor remains a separately controlled future cleanup candidate.

## 5. Native Cauchy/H6 comparison robustness

The independently completed Cauchy real route and H6 complex research producer
can instantiate the generic comparison boundary directly:

```text
R-ROUTE-C / completed RCBOMA
   ↓
H6 Cauchy-native C producer
   ↓
C-COMPARE-BLOCK-001 generic comparison contract
```

The verified native closure excludes H5 `cToD` transport and selected
Dedekind/RStage-integration implementation dependencies.

Permanent conclusion:

```text
H5/Dedekind transport is not mathematically required by C-CL-COMPARE-001 when
the comparison layer is factored through the verified scalar/coordinate
interface.
```

H6 remains non-accepted.

## 6. Relation/function firewall

The permanent comparison contract preserves:

```text
relation totality + uniqueness
!=
global selected comparison function
```

Functional maps require explicit `CoordinateExtractor` data. No global
coordinate or inverse selector is part of the canonical comparison boundary.

## 7. Permanent Route-Q branch — ST2-EXP-002

```text
C-DP-001
   ├── C-ROUTE-P / selected producer ─────┐
   │                                      │
   │                                      ▼
   │                            ST2-EXP-002-PQ-J-001
   │                            R-FIELD-ISOMORPHISM
   │                                      ▲
   │                                      │
   └── C-ROUTE-Q / verified alternative ──┘
```

`ST2-EXP-002-PQ-J-001` is a permanent verified alternative-construction
Junction, not the accepted integration Junction and not a replacement for
`C-J-001`.

```text
C-ROUTE-P   SELECTED / accepted producer path
C-ROUTE-Q   PERMANENT VERIFIED ALTERNATIVE / NON-SELECTED / NOT ACCEPTED EXPORT
```

## 8. Accepted C meaning

The accepted C semantics remain:

```text
faithful accepted-R embedding
commutative-field-strength behavior
distinguished I
I² = -1
every element has an a+bI expression
that expression is unique
non-real-collapse consequence
representation-neutral comparison adequacy
same-carrier integration
```

No algebraic closure, FTA, analytic completeness, total order on C, or built-in
Complex identity is silently imported by acceptance.

Accepted ownership remains:

```text
C-BLOCK-001          selected seven-core-Claim producer
C-COMPARE-BLOCK-001  C-CL-COMPARE-001
C-J-001              C-CL-INTEGRATION-001
C-BLOCK-002          final accepted export
CA-20                ACCEPT
```

## 9. Learning provenance

```text
ST2-EXP-001  CLOSED / PASS
  → exact sixteen-property production R→C dependency

ST2-EXP-002  CLOSED / PASS
  → complete C-ROUTE-Q + P/Q non-acceptance Junction

ST2-EXP-003  CLOSED / PASS / H1-H6 EXACT
  → complete Cauchy R + R Junction + H6 downstream robustness

ST2-EXP-011  CLOSED / PASS
  → scalar-generic comparison boundary
  → accepted RBOMA semantic preservation
  → native RCBOMA/H6 comparison without H5 transport
  → relation/function firewall retained
```

Frozen Plans, failures, exact evidence, and Study/Act records remain in the
Learning Graph.

## 10. Current C boundary

```text
selected upstream R export             R-BLOCK-001 / Dedekind
production R→C mathematical contract   exact sixteen-property surface
C-DP-001                                RESOLVED / SELECTS C-ROUTE-P
selected C producer                     C-BLOCK-001
comparison scalar contract              zero/one/neg/add/mul + coordinate laws
comparison Claim owner                  C-COMPARE-BLOCK-001
accepted C integration                  C-J-001
accepted C export                       C-BLOCK-002 / CA-20
permanent C alternative                 C-ROUTE-Q
alternative-construction Junction       ST2-EXP-002-PQ-J-001
Cauchy-native H6 evidence               retained / non-accepted
ST2-EXP-011                             CLOSED / PASS / LESSON INTEGRATED
ACTIVE STAGE-TWO EXPERIMENT             NONE
NEXT OWNER-SEQUENCED EXPERIMENT         ST2-EXP-004 / NOT ACTIVE / NO FROZEN PLAN
```
