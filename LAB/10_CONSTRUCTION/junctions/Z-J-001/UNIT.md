# Z-J-001 — Integer Representation Convergence
- **Status:** OPEN / PENDING

PASS requires independent signed/pair routes, exact signed round trip, pair round trip up to `~`, and:
```text
p ~ q ↔ normalize(p)=normalize(q).
```
No canonical integer representation may be selected before this Junction passes.
