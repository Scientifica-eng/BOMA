# Bricks

Canonical atomic construction units.

Every Brick must have a unique ID, explicit inputs/outputs, construction rule, invariants, verification status, epistemic status, dependencies, and sensitivity.