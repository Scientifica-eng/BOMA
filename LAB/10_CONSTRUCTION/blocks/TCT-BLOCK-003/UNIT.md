# TCT-BLOCK-003 — Canonical Decomposition

## Canonical Identity

- **Unit ID:** `TCT-BLOCK-003`
- **Type:** Block
- **Layer:** `L1 — explicit constructed structure`
- **Operational Status:** **ACTIVE**
- **Epistemic Status:** **MIXED — CONSTRUCTED DECOMPOSITION JUDGMENT + DERIVED CANONICALITY GUARANTEE**
- **PDSA origin:** `PDSA-TCT-005`

## Purpose

Package the verified terminal-recovery result into a reusable canonical decomposition contribution for the current TCT path without introducing numerical depth, successor, a global predecessor function, ordered-pair objects, or quotient classes.

## Inputs

```text
TCT-BLOCK-002  selected normal-form presentations + meta-level recovery
TCT-BR-010     reassociation-generated structural equivalence ≈
TCT-BR-009     terminal preservation DERIVED under BR-010
TCT-J-001      PASS / RESOLVED
```

## Selected normal-form environment

```text
NF  ::= ε | NF+
NF+ ::= U | (NF+ ⊙ U)
```

The grammar is a meta-level representation convention under the active pre-numerical metatheory contract.

## Decomposition judgment

Use the schematic meta-structural judgment:

```text
P ◁U A
```

read:

> `P` is the recovered predecessor presentation of the non-empty selected normal-form presentation `A`, relative to the distinguished terminal role `U`.

The judgment is generated by:

```text
D-BASE
ε ◁U U

D-STEP
P ◁U (P ⊙ U)     for P ∈ NF+
```

This notation is not an ordered-pair object and is not a global object-level function.

## Constructed content

### CD-1 — Presentation-level decomposition existence

Every explicitly supplied non-empty selected normal-form presentation admits one decomposition judgment by grammar inspection.

### CD-2 — Presentation-level determinism

Within the selected normal form:

```text
U
```

recovers predecessor `ε`, while:

```text
P ⊙ U
```

recovers the displayed predecessor `P`.

The refined grammar excludes the historical definitional duplicate `ε ⊙ U` from non-empty selected forms.

## Derived canonicality contribution

### CD-3 — Invariance modulo structural equivalence

If:

```text
P ◁U A
Q ◁U B
A ≈ B
```

then:

```text
P ≈ Q
```

and the terminal roles correspond to the same distinguished `U` role.

This result is derived from `TCT-BR-010` through the terminal-recovery proof of `PDSA-TCT-004` and the resolved Junction `TCT-J-001`.

### Meaning of “canonical”

In this Block, **canonical decomposition** means:

> the recovered predecessor contribution is well-defined up to the declared structural equivalence `≈` across equivalent selected presentations.

It does **not** mean that an equivalence-class object has been constructed.

## Reusable certified contribution

The Block exports the following downstream guarantee:

> **Canonical Decomposition Guarantee:** For any explicitly supplied non-empty selected normal-form presentation on the current TCT path, terminal predecessor recovery exists; and equivalent presentations under `TCT-BR-010` recover equivalent predecessor presentations with corresponding distinguished terminal `U` roles.

The guarantee is distinct from the Block document and from the meta-level `recover` procedure that witnesses it.

This preserves the project distinction:

```text
Construction Unit ≠ Reusable Certified Contribution
```

## What this Block does not construct

```text
pred : Config → Config
pred : Config/≈ → Config/≈
a quotient Config/≈
a completed Config carrier
ordered-pair objects
numerical depth
number of construction steps
successor
natural numbers
induction
arithmetic
order
```

Any later introduction of such structure requires its own explicit unit/boundary.

## Verification evidence

Primary:

`LAB/PDSA/experiments/PDSA-TCT-005-CANONICAL-DECOMPOSITION-VERIFICATION-001.md`

Inherited theorem evidence:

`LAB/PDSA/experiments/PDSA-TCT-004-TERMINAL-RECOVERY-PROOF-001.md`

Resolved Junction:

`LAB/10_CONSTRUCTION/junctions/TCT-J-001/UNIT.md`

## Acceptance result

```text
EXISTENCE                         PASS
DETERMINISM ON SELECTED NF        PASS
INVARIANCE MODULO ≈               PASS
TERMINAL ROLE CORRESPONDENCE      PASS
NO HIDDEN QUOTIENT                PASS
NO HIDDEN ORDERED PAIR            PASS
NO HIDDEN GLOBAL PRED FUNCTION    PASS
NO NUMERICAL DEPENDENCE           PASS
```

**Block result: ACTIVE / ACCEPTED on the current path.**

## Downstream

`TCT-BLOCK-004 — Construction Depth / Structural Iteration` is now the next reserved target.

The immediate downstream question must not begin by assigning a natural-number-valued depth. It should ask what **structural iteration invariant** can be built from repeated canonical decomposition without numerical circularity.

## Sensitivity

Reopen/retest this Block if any of the following changes:

```text
TCT-BLOCK-002 selected grammar
TCT-BR-010 structural equivalence
empty definitional convention
terminal role / kernel U
TCT-BR-009 theorem premises
TCT-J-001 status
recovery interpretation
```

A future branch may preserve the same certified contribution through a different construction route; such a producer should receive its own unit identity and convergence evidence.

## Provenance

Constructed and verified through:

```text
PDSA-TCT-003
PDSA-TCT-004
PDSA-TCT-005
```

Historical predecessor work remains preserved under the earlier TCT and reverse-engineering records.
