name: BOMA N-Core R-B — Claim-Level V5 (legacy manual)

on:
  workflow_dispatch:

# Historical first claim-level workflow.
# Superseded for active verification by:
#   .github/workflows/boma-ncore-rb-005-evidence.yml
# It remains manual-only for provenance and must not be used as current V5 evidence.

jobs:
  legacy-note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain supersession
        run: |
          echo "This workflow is retained as provenance only."
          echo "Use boma-ncore-rb-005-evidence.yml for current claim-level V5 verification."
