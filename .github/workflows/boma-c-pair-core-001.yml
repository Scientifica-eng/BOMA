name: BOMA C Selected Pair Ring Core 001 — V5

permissions:
  contents: read

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - 'LAB/payloads/lean/NCore/**'
      - 'LAB/payloads/lean/NArithmetic/**'
      - 'LAB/payloads/lean/ZStage/**'
      - 'LAB/payloads/lean/QStage/**'
      - 'LAB/payloads/lean/RStage/**'
      - 'LAB/payloads/lean/CStage/CRInterfaceNoSquareProbe001.lean'
      - 'LAB/payloads/lean/CStage/CRoutePairProbe001.lean'
      - 'LAB/payloads/lean/CStage/CPairCore001.lean'
      - 'LAB/20_FORMALIZATION/R_STAGE/R_INTEGRATION_002_INPUTS.txt'
      - 'LAB/20_FORMALIZATION/C_STAGE/C_R_INTERFACE_PROBE_001_INPUTS.txt'
      - 'LAB/20_FORMALIZATION/C_STAGE/C_ROUTE_PAIR_PROBE_001_INPUTS.txt'
      - 'LAB/20_FORMALIZATION/C_STAGE/C_PAIR_CORE_001_INPUTS.txt'
      - '.github/workflows/boma-c-pair-core-001.yml'
      - 'lean-toolchain'
  push:
    branches:
      - 'feature/c-stage-*'
    paths:
      - 'LAB/payloads/lean/NCore/**'
      - 'LAB/payloads/lean/NArithmetic/**'
      - 'LAB/payloads/lean/ZStage/**'
      - 'LAB/payloads/lean/QStage/**'
      - 'LAB/payloads/lean/RStage/**'
      - 'LAB/payloads/lean/CStage/CRInterfaceNoSquareProbe001.lean'
      - 'LAB/payloads/lean/CStage/CRoutePairProbe001.lean'
      - 'LAB/payloads/lean/CStage/CPairCore001.lean'
      - 'LAB/20_FORMALIZATION/R_STAGE/R_INTEGRATION_002_INPUTS.txt'
      - 'LAB/20_FORMALIZATION/C_STAGE/C_R_INTERFACE_PROBE_001_INPUTS.txt'
      - 'LAB/20_FORMALIZATION/C_STAGE/C_ROUTE_PAIR_PROBE_001_INPUTS.txt'
      - 'LAB/20_FORMALIZATION/C_STAGE/C_PAIR_CORE_001_INPUTS.txt'
      - '.github/workflows/boma-c-pair-core-001.yml'
      - 'lean-toolchain'

concurrency:
  group: boma-c-pair-core-001-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify:
    name: verify selected Route P canonical ring core
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Verify exact selected-source provenance
        shell: bash
        run: |
          set -euo pipefail
          expected_sha="${{ github.event.pull_request.head.sha || github.sha }}"
          actual_sha="$(git rev-parse HEAD)"
          test "$actual_sha" = "$expected_sha"
          {
            printf '## BOMA C selected Route P ring core\n'
            printf -- '- Event: `%s`\n' "$GITHUB_EVENT_NAME"
            printf -- '- Run ID: `%s`\n' "$GITHUB_RUN_ID"
            printf -- '- Verified source commit: `%s`\n' "$actual_sha"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Setup Lean
        uses: leanprover/lean-action@v1
        with:
          auto-config: false
          build: false
          test: false
          lint: false
          use-github-cache: false
          use-mathlib-cache: false

      - name: Validate exact selected-route assembly and boundaries
        shell: bash
        run: |
          set -euo pipefail
          manifests=(
            LAB/20_FORMALIZATION/R_STAGE/R_INTEGRATION_002_INPUTS.txt
            LAB/20_FORMALIZATION/C_STAGE/C_R_INTERFACE_PROBE_001_INPUTS.txt
            LAB/20_FORMALIZATION/C_STAGE/C_ROUTE_PAIR_PROBE_001_INPUTS.txt
            LAB/20_FORMALIZATION/C_STAGE/C_PAIR_CORE_001_INPUTS.txt
          )
          for manifest in "${manifests[@]}"; do
            test -s "$manifest"
            while IFS= read -r src || [ -n "$src" ]; do
              [ -z "$src" ] && continue
              test -f "$src"
            done < "$manifest"
          done
          selected=LAB/payloads/lean/CStage/CPairCore001.lean
          test "$(cat LAB/20_FORMALIZATION/C_STAGE/C_PAIR_CORE_001_INPUTS.txt)" = "$selected"
          forbidden='RouteQuadraticQuotientProbe001|LowerCut|CutEquiv|Classical\.|dedekindLUB|rationalDensity|archimedeanUpper|(^|[^[:alnum:]_])(sorry|axiom)([^[:alnum:]_]|$)'
          if grep -En "$forbidden" "$selected"; then
            printf 'Selected core contains a forbidden dependency or logical placeholder\n' >&2
            exit 1
          fi

      - name: Verify the exact abstract-interface selected pair ring core
        shell: bash
        run: |
          set -euo pipefail
          target=/tmp/BOMACSelectedPairRingCore001.lean
          : > "$target"
          for manifest in \
            LAB/20_FORMALIZATION/R_STAGE/R_INTEGRATION_002_INPUTS.txt \
            LAB/20_FORMALIZATION/C_STAGE/C_R_INTERFACE_PROBE_001_INPUTS.txt \
            LAB/20_FORMALIZATION/C_STAGE/C_ROUTE_PAIR_PROBE_001_INPUTS.txt \
            LAB/20_FORMALIZATION/C_STAGE/C_PAIR_CORE_001_INPUTS.txt; do
            while IFS= read -r src || [ -n "$src" ]; do
              [ -z "$src" ] && continue
              cat "$src" >> "$target"
              printf '\n' >> "$target"
            done < "$manifest"
          done
          lake env lean "$target"
