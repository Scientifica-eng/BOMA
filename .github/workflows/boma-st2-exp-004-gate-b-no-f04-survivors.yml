name: BOMA ST2-EXP-004 Gate B — No-F04 R Survivors

permissions:
  contents: read

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
  push:
    branches:
      - 'feature/stage-two-st2-exp-004-*'

concurrency:
  group: boma-st2-exp-004-historical-gate-b-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify_historical_gate:
    name: verify immutable historical Gate B
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0

      - name: Verify read-only historical Gate B
        shell: bash
        run: |
          set -euo pipefail
          python3 LAB/00_ARCHITECTURE/tools/st2_exp_004_historical_gate_sentinel.py \
            --gate B \
            --expected-head '${{ github.event.pull_request.head.sha || github.sha }}' \
            --json-out /tmp/st2-exp-004-historical-gate-b.json
          test -z "$(git status --porcelain=v1)"

      - name: Upload historical Gate B sentinel
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: st2-exp-004-historical-gate-b-${{ github.run_id }}
          path: /tmp/st2-exp-004-historical-gate-b.json
          if-no-files-found: warn
